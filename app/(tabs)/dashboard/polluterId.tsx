import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const polluterId = () => {
  return (
    <View>
      <Text>polluterId</Text>
    </View>
  )
}

export default polluterId

const styles = StyleSheet.create({})